# Reject: nested sensitive key "api_key" / "token" family (SPEC §11.1).
policy_profile: demo_readonly
datasets:
  sales_inventory_daily:
    physical_table: v_sales_inventory_daily
    title: Sales
    description: demo
    api_key: "NESTED_SECRET_VALUE_XYZ"
    capabilities:
      - READ_DETAIL
    default_limit: 50
    max_limit: 100
    allowed_fields:
      product_id:
        physical_column: product_id
