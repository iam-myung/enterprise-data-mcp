# Reject: top-level sensitive key "password" (SPEC §11.1).
policy_profile: demo_readonly
password: "SHOULD_NOT_LEAK_IN_ERROR"
datasets:
  sales_inventory_daily:
    physical_table: v_sales_inventory_daily
    title: Sales
    description: demo
    capabilities:
      - READ_DETAIL
    default_limit: 50
    max_limit: 100
    allowed_fields:
      product_id:
        physical_column: product_id
