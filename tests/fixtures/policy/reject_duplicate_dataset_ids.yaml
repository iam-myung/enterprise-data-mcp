# Reject: duplicate dataset_id via list form (SPEC §11.1 / Step 4-PLAN).
policy_profile: demo_readonly
datasets:
  - dataset_id: sales_inventory_daily
    physical_table: v_sales_inventory_daily
    title: Sales A
    description: first
    capabilities:
      - READ_DETAIL
    default_limit: 50
    max_limit: 100
    allowed_fields:
      product_id:
        physical_column: product_id
  - dataset_id: sales_inventory_daily
    physical_table: v_other
    title: Sales B
    description: duplicate id
    capabilities:
      - READ_DETAIL
    default_limit: 50
    max_limit: 100
    allowed_fields:
      product_id:
        physical_column: product_id
