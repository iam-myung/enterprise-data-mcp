# Placeholder only — no real secrets (SPEC §12).
APP_ENV=local
MCP_TRANSPORT=stdio
MCP_CONTAINER=demo
MCP_CALLER_ID=demo-stdio-client
MCP_POLICY_PROFILE=demo_readonly
ACCESS_POLICY_PATH=config/access_policy.example.yaml
BUSINESS_TIMEZONE=Asia/Shanghai
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3307
MYSQL_USER=edmcp_ro
MYSQL_PASSWORD=changeme
MYSQL_DATABASE=edmcp_demo
MYSQL_QUERY_TIMEOUT_MS=5000
MYSQL_WORKER_LIMIT=4
AUDIT_DB_PATH=var/audit.sqlite3
AUDIT_STALE_STARTED_SECONDS=120
# Demo CLI (BUG-03) — DEMO_MODEL=deterministic for offline; openai|real needs MODEL_*
DEMO_MODEL=deterministic
MODEL_API_KEY=
MODEL_BASE_URL=
MODEL_NAME=
DEMO_MCP_TRANSPORT=stdio
