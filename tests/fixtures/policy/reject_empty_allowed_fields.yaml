# Reject: empty allowed_fields (DatasetPolicy invariant / §11.1).
policy_profile: demo_readonly
datasets:
  sales_inventory_daily:
    physical_table: v_sales_inventory_daily
    title: Sales
    description: demo
    capabilities:
      - READ_DETAIL
    default_limit: 50
    max_limit: 100
    allowed_fields: {}
